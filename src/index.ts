/**
 * Test cases to pass:
 * 1. Skeleton function to be accessible through test
 * 2. Correction: numbers = "" i.e string length is 0
 */

export function add(numbers: string) {
    let calculatedNum = 0;
    // correction for numbers = ""
    return calculatedNum;
}
